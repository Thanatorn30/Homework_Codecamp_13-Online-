        // -------------Lab1----------------------
        // * มีค่าเป็น 4 เพราะ shoppingCart.push('Banana') เป็นการเพิ่ม element ที่มาจากการ copy by ref

        // -------------Lab2----------------------
        // * มีค่าเป็น a,b,function(){ alert(this)}

        // ---------Lab3------------------------
        // let arr = ['Dog','Cat','Rat']
        // arr.push('Fish')
        // arr.unshift('Horse')
        // arr.push('Giraffe','Snake')
        // arr.shift()
        // arr.pop()
        // arr.shift()
        // console.log(arr);
        // จะได้['Cat', 'Rat', 'Fish', 'Giraffe']